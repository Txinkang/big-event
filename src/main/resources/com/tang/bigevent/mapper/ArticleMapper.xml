<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tang.bigevent.mapper.ArticleMapper">
    <select id="list" resultType="com.tang.bigevent.pojo.Article">
        select * from article
         <where>
             <if test="categoryId!=null">
                 category_id=#{categoryId}
             </if>
            <if test="state!=null">
                and state=#{state}
            </if>
            and create_user=#{userid}
         </where>
    </select>

    <update id="update">
        update article set
            <if test="title!=null">
                title=#{title},
            </if>
            <if test="content!=null">
                content=#{content},
            </if>
            <if test="coverImg!=null">
                cover_img=#{coverImg},
            </if>
            <if test="state!=null">
                state=#{state},
            </if>
            <if test="categoryId!=null">
                category_id=#{categoryId},
            </if>
            update_time=#{updateTime} where id=#{id};
    </update>
</mapper>